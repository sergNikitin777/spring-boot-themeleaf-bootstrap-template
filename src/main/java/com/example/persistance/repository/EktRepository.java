package com.example.persistance.repository;

import com.example.persistance.entity.Ekt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EktRepository extends JpaRepository<Ekt, Integer> {
}